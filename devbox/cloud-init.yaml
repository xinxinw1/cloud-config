#cloud-config

# Update and upgrade all packages on first boot
package_update: true
package_upgrade: true

packages:
  - neovim

users:
  - default
  - name: xinxin
    gecos: xinxin
    shell: /bin/bash
    groups: [sudo]
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]

write_files:
  - path: /home/xinxin/setup.sh
    permissions: '0755'
    content: |
      #!/bin/bash
      set -e

      echo "=== Authorizing GitHub CLI for xinxinw1 ==="
      echo "Paste a GitHub personal access token (repo, admin:public_key scopes) and press enter:"
      read -s -p "Token: " GH_TOKEN
      echo
      echo "$GH_TOKEN" | gh auth login --hostname github.com --git-protocol ssh --with-token
      unset GH_TOKEN

      echo "=== Adding SSH key to GitHub account ==="
      gh ssh-key add ~/.ssh/id_rsa.pub --title "$(hostname)-$(date +%Y%m%d)"

      echo "=== Cloning repos into ~/git ==="
      mkdir -p ~/git
      git clone git@github.com:xinxinw1/xin-xin.me.git ~/git/xin-xin.me
      git clone git@github.com:xinxinw1/cloud-config.git ~/git/cloud-config

      echo "=== Setup complete ==="

runcmd:
  # Fix ownership/permissions on setup.sh now that xinxin exists
  - chown xinxin:xinxin /home/xinxin/setup.sh
  - chmod 0755 /home/xinxin/setup.sh

  # Give xinxin the same authorized_keys as root
  - mkdir -p /home/xinxin/.ssh
  - cp /root/.ssh/authorized_keys /home/xinxin/.ssh/authorized_keys
  - chown -R xinxin:xinxin /home/xinxin/.ssh
  - chmod 700 /home/xinxin/.ssh
  - chmod 600 /home/xinxin/.ssh/authorized_keys

  # Generate a 4096-bit RSA SSH key for xinxin (no passphrase)
  - sudo -u xinxin ssh-keygen -t rsa -b 4096 -f /home/xinxin/.ssh/id_rsa -N "" -C "xinxin@$(hostname)"

  # Install GitHub CLI (gh) via the official apt repo
  - curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg -o /usr/share/keyrings/githubcli-archive-keyring.gpg
  - chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
  - echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" > /etc/apt/sources.list.d/github-cli.list
  - apt-get update
  - apt-get install -y gh

  - echo "Droplet bootstrap complete. SSH in as xinxin and run ~/setup.sh to finish GitHub setup and clone the repo." > /home/xinxin/BOOTSTRAP_DONE
  - chown xinxin:xinxin /home/xinxin/BOOTSTRAP_DONE

# Reboot automatically once cloud-init finishes, but only if the
# package upgrades actually left a restart pending
power_state:
  mode: reboot
  condition: test -f /var/run/reboot-required
  timeout: 60
